<html lang='en'>
    <head>
        <meta charset='UTF-8' />
        <meta http-equiv='X-UA-Compatible' content='IE=edge' />
        <meta name='viewport' content='width=device-width, initial-scale=1.0' />
        <title>🤷 {{shortCode}}</title>
        <link rel='preconnect' href='https://fonts.googleapis.com' />
        <link rel='preconnect' href='https://fonts.gstatic.com' crossorigin />
        <link href='https://fonts.googleapis.com/css2?family=Martian+Mono&display=swap' rel='stylesheet' />
        <style>
            html,body { margin: 0; padding: 0;font-family: 'Martian Mono', monospace; background-color: #111119; color: white;} a
            { color: #00ff94; display: block; } p {color: #696969} body {display: flex; flex-direction: column; margin: auto;
            max-width: 500px} p {margin-top: 50px;} h1 {margin-top: 0; margin-bottom: 50px;} span {color: #e66b6b} sub { color:
            #696969; font-size: x-small; margin-bottom: 20px; margin-top: 5px; text-overflow: ellipsis; max-width: 200px;
            white-space: nowrap; overflow: hidden;}
        </style>
    </head>
    <body>
        <p><span>{{shortCode}}</span> did not match any short codes</p>
        <h1>Did you mean</h1>
        {{#each scoredMatches}}
            <a href='/go/{{shortCode}}'>{{shortCode}}</a>
            <sub>{{url}}</sub>
        {{/each}}
    </body>
</html>